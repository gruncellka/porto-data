{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/gruncellka/porto-data/refs/heads/main/porto_data/schemas/providers.schema.json",
  "title": "Provider registry",
  "description": "Domain registry for postal providers. Folder providers/<id>/ is implementation storage only; authoritative ids and coarse metadata live here. IANA timezone for a provider is policy/jurisdictions.json \u2192 jurisdictions[country].timezone (not duplicated here). Product-level mark_type and tracking_mode remain the execution source of truth.",
  "type": "object",
  "required": [
    "providers"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri"
    },
    "providers": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": [
          "name",
          "country",
          "mark_types"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Display name"
          },
          "country": {
            "type": "string",
            "pattern": "^[A-Z]{2}$",
            "description": "Primary country (ISO 3166-1 alpha-2). Timezone: policy/jurisdictions.json jurisdictions[this code].timezone."
          },
          "mark_types": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "enum": [
                "stamp",
                "label"
              ]
            },
            "description": "Coarse overview; exact execution comes from each product"
          },
          "tracking_model": {
            "type": "string",
            "enum": [
              "mixed",
              "included",
              "none",
              "optional"
            ],
            "description": "Optional coarse provider-level tracking posture; products define tracking_mode precisely."
          }
        },
        "additionalProperties": true
      }
    }
  },
  "additionalProperties": false
}
