{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/gruncellka/porto-data/refs/heads/main/porto_data/schemas/graph.schema.json",
  "title": "Graph schema",
  "description": "Provider graph: data file dependencies, resolution edges (products × zone × weight; marks × zone × service), and service list",
  "type": "object",
  "required": [
    "file_type",
    "strategy",
    "unit",
    "dependencies",
    "edges",
    "services"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "JSON Schema URI reference"
    },
    "file_type": {
      "type": "string",
      "const": "graph",
      "description": "File type identifier"
    },
    "strategy": {
      "type": "string",
      "enum": [
        "service",
        "id",
        "speed",
        "min"
      ],
      "description": "Product disambiguation policy when multiple native products share a porto_id (catalog fact — SDK reads, does not hardcode per provider)"
    },
    "unit": {
      "type": "object",
      "required": [
        "weight",
        "dimension",
        "price",
        "currency"
      ],
      "properties": {
        "weight": {
          "type": "string",
          "const": "g"
        },
        "dimension": {
          "type": "string",
          "const": "mm"
        },
        "price": {
          "type": "string",
          "const": "cents"
        },
        "currency": {
          "type": "string",
          "enum": [
            "EUR",
            "CHF",
            "UAH",
            "USD"
          ],
          "description": "ISO 4217 currency code; must match policy/markets.json markets[providers.json country].currency"
        }
      },
      "additionalProperties": false
    },
    "dependencies": {
      "type": "object",
      "patternProperties": {
        "^[a-z_]+$": {
          "type": "object",
          "required": [
            "file",
            "depends_on",
            "description"
          ],
          "properties": {
            "file": {
              "type": "string",
              "pattern": "^(policy/[a-z0-9_./-]+\\.json|formats/[a-z0-9_./-]+\\.json|[a-z0-9_./-]+\\.json)$",
              "description": "Data path: provider-relative (e.g. prices/products.json) or bundle-shared under policy/ or formats/"
            },
            "depends_on": {
              "type": "array",
              "items": {
                "type": "string",
                "pattern": "^(policy/[a-z0-9_./-]+\\.json|formats/[a-z0-9_./-]+\\.json|[a-z0-9_./-]+\\.json)$"
              },
              "description": "List of dependent files (same path convention as file)"
            },
            "description": {
              "type": "string",
              "minLength": 1,
              "description": "Dependency description"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "edges": {
      "type": "object",
      "required": [
        "products",
        "marks"
      ],
      "properties": {
        "products": {
          "type": "object",
          "description": "Per-product edges: each key is a **product `id`** from products.json. Maps product → allowed zones and weight tiers.",
          "patternProperties": {
            "^[a-z_]+$": {
              "type": "object",
              "required": [
                "zones",
                "weight_tiers"
              ],
              "properties": {
                "zones": {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "type": "string",
                    "pattern": "^[a-z0-9_]+$"
                  },
                  "description": "Available zones for this product"
                },
                "weight_tiers": {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "type": "string",
                    "pattern": "^W[0-9]+$"
                  },
                  "description": "Available weight tiers for this product"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        "marks": {
          "type": "object",
          "description": "Mark profile resolution per zone. Keys are zone ids from zones.json.",
          "patternProperties": {
            "^[a-z0-9_]+$": {
              "type": "object",
              "required": [
                "profile"
              ],
              "properties": {
                "profile": {
                  "type": "string",
                  "pattern": "^[a-z0-9_]+$",
                  "description": "Default marks.profiles[].id for this zone"
                },
                "services": {
                  "type": "object",
                  "minProperties": 1,
                  "patternProperties": {
                    "^[a-z_]+$": {
                      "type": "string",
                      "pattern": "^[a-z0-9_]+$"
                    }
                  },
                  "additionalProperties": false,
                  "description": "Native service id → marks.profiles[].id when that service is selected"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        "wire": {
          "type": "object",
          "description": "Adapter catalog codes per integration × product × zone. Optional services map for service-composed SKUs (e.g. Deutsche Post Internetmarke).",
          "patternProperties": {
            "^[a-z_]+$": {
              "type": "object",
              "patternProperties": {
                "^[a-z_]+$": {
                  "type": "object",
                  "patternProperties": {
                    "^[a-z0-9_]+$": {
                      "$ref": "#/definitions/wire_zone_entry"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "services": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^[a-z_]+$"
      },
      "description": "Attachable services for this provider. Each entry must match **services.json `id`** (provider-native key)."
    }
  },
  "definitions": {
    "wire_code": {
      "description": "Operator adapter wire identifier (integer productCode or string catalog key). null = TBD until catalog harvest.",
      "oneOf": [
        {
          "type": "integer",
          "minimum": 1
        },
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "null"
        }
      ]
    },
    "wire_zone_entry": {
      "type": "object",
      "required": [
        "base"
      ],
      "properties": {
        "base": {
          "$ref": "#/definitions/wire_code"
        },
        "services": {
          "type": "object",
          "description": "Composite adapter code when this service is selected with the base product in this zone.",
          "patternProperties": {
            "^[a-z_]+$": {
              "$ref": "#/definitions/wire_code"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
