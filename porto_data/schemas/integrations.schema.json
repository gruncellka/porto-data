{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/gruncellka/porto-data/refs/heads/main/porto_data/schemas/integrations.schema.json",
  "title": "Provider execution integrations",
  "description": "Per-operator execution adapter manifest. Declares the active wire integration id and SDK execution capabilities (billing, mark purchase). Wire product codes remain in graph.json edges.wire; credentials are runtime config only.",
  "type": "object",
  "required": [
    "file_type",
    "provider",
    "adapter",
    "capabilities"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "JSON Schema URI reference"
    },
    "file_type": {
      "type": "string",
      "const": "integrations",
      "description": "File type identifier"
    },
    "provider": {
      "type": "string",
      "pattern": "^[a-z_]+$",
      "description": "Provider id (must match path providers/<id>/)"
    },
    "adapter": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$",
      "description": "Wire integration id; must match a key under graph.json edges.wire for this operator"
    },
    "capabilities": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "mark_purchase_sync",
          "wallet_balance_read"
        ],
        "description": "Execution capability exposed by the SDK when this integration is active"
      }
    }
  },
  "additionalProperties": false
}
